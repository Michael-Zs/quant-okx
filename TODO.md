对于策略组/实盘部署
只能保存，但是无法修改，或者看保存的配置。
也没有回测

---

默认backtest返回点太密集。最好默认30个点
（现在的点是平均取样吗？

---

✅ 多个部署打架 —— 已解决（daemon 只写 intent + 唯一 executor 聚合对账下单：多部署共享账户时，各部署只产出目标信号，由 executor 按资本份额聚合后统一下单，不再互相覆盖仓位）

---
多币策略还是不可用
